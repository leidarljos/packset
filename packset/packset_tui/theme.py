"""Tokyo Night Storm CSS paths. Shared snippet first, widget layout second."""

from pathlib import Path

TUI_DIR = Path(__file__).resolve().parent
CSS_FILES = [TUI_DIR / "tokyo_night_storm.tcss", TUI_DIR / "storm.tcss"]
